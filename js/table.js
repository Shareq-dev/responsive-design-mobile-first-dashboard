document.addEventListener('DOMContentLoaded',()=>{const search=document.querySelector('#user-search'),rows=[...document.querySelectorAll('#users-body tr')],empty=document.querySelector('#table-empty');if(!search)return;search.addEventListener('input',()=>{const query=search.value.toLowerCase().trim();let visible=0;rows.forEach(row=>{const match=row.textContent.toLowerCase().includes(query);row.hidden=!match;if(match)visible++});empty.hidden=visible!==0})});
